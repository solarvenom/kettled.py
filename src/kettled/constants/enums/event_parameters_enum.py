from .enums_superclass import EnumSuperclass

class EVENT_PARAMETERS_ENUM(EnumSuperclass):
    EVENT_NAME = "event_name"
    DATE_TIME = "date_time"
    RECURRENCY = "recurrency"
    FALLBACK_DIRECTIVE = "fallback_directive"
    CALLBACK = "callback"